# Implementation Task

## Objective
Create a Python script that prints the string "bonjour" to the standard output.

## Context
The goal is to implement a basic Python functionality as specified in ticket KAN-1 to output a greeting message.

## Requirements
- Use the Python programming language.
- Implement a mechanism to print exactly "bonjour".

## Implementation
1. Inspect the project structure to determine the appropriate location for the script.
2. Create a new Python file (e.g., `main.py` or `bonjour.py`).
3. Implement the `print("bonjour")` statement within the file.
4. Ensure the code follows Python idiomatic standards.

## Acceptance Criteria
- [ ] The implementation is written in Python.
- [ ] Executing the script outputs exactly "bonjour" to the console.

## Validation
1. Run the created Python script using the command: `python <filename>.py`.
2. Verify that the output in the terminal is exactly `bonjour`.

## Final Report
- List the files created or modified.
- Confirm that the output matches the acceptance criteria.